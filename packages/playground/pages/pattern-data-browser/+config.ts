import type { Config } from 'vike/types';

export default {
  title: 'Data Browser | Virtual Scroll',
  description: 'Grouped directory pattern combining iOS-style sticky section headers, instant search with a rebuilt section index and jump-to navigation with configurable alignment.',
} satisfies Config;
