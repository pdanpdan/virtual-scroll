import type { Config } from 'vike/types';

export default {
  title: 'Data Browser | Virtual Scroll',
  description: 'Grouped directory browser with sticky section headers, instant search and jump-to navigation.',
} satisfies Config;
