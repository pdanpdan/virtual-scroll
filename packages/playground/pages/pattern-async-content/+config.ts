import type { Config } from 'vike/types';

export default {
  title: 'Async Content | Virtual Scroll',
  description: 'Virtualized feed whose rows fetch simulated content on demand, with a shared cache and auto-measured heights.',
} satisfies Config;
