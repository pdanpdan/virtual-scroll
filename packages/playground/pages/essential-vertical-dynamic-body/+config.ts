import type { Config } from 'vike/types';

export default {
  title: 'Vertical Dynamic Body | Virtual Scroll',
  description: 'Uses the main browser window for scrolling dynamic items with sizes automatically detected via ResizeObserver.',
} satisfies Config;
