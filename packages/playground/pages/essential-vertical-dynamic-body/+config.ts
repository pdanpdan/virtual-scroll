import type { Config } from 'vike/types';

export default {
  title: 'Vertical Dynamic Body | Virtual Scroll',
  description: 'Variable-height rows scrolled by the browser window, measured with ResizeObserver.',
} satisfies Config;
