import type { Config } from 'vike/types';

export default {
  title: 'Spreadsheet Grid | Virtual Scroll',
  description: 'Excel-like bidirectional grid with sticky row and column headers and resizable cells.',
} satisfies Config;
