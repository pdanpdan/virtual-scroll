import type { Config } from 'vike/types';

export default {
  title: 'Configurator | Virtual Scroll',
  description: 'Select the features you need and generate complete typed Vue code: a component, a composable, or a CodePen.',
} satisfies Config;
