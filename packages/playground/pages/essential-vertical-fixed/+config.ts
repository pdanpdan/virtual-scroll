import type { Config } from 'vike/types';

export default {
  title: 'Vertical Fixed | Virtual Scroll',
  description: 'Optimized for lists where every item has the same height and items are only rendered when they enter the visible viewport.',
} satisfies Config;
