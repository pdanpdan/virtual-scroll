import type { Config } from 'vike/types';

export default {
  title: 'Vertical Fixed | Virtual Scroll',
  description: 'Vertical list of uniform-height rows; only the rows in the viewport are rendered.',
} satisfies Config;
