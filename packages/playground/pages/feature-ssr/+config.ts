import type { Config } from 'vike/types';

export default {
  title: 'SSR Support | Virtual Scroll',
  description: 'Demonstrates the ssrRange prop for pre-rendering content on the server and smooth client-side hydration.',
} satisfies Config;
