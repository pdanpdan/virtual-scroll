import type { Config } from 'vike/types';

export default {
  title: 'SSR Support | Virtual Scroll',
  description: 'Pre-renders a range on the server and scrolls to it after hydration.',
} satisfies Config;
