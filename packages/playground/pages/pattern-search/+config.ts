import type { Config } from 'vike/types';

export default {
  title: 'Search & Highlight | Virtual Scroll',
  description: 'Search the whole dataset and highlight matches in the mounted rows with the CSS Custom Highlight API (Ctrl+K).',
} satisfies Config;
