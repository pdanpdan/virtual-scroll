import type { Config } from 'vike/types';

export default {
  title: 'Layout Switcher | Virtual Scroll',
  description: 'One dataset rendered as a virtualized list, grid or data table without reshaping the data.',
} satisfies Config;
