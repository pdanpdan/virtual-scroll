import type { Config } from 'vike/types';

export default {
  title: 'Masonry Gallery | Virtual Scroll',
  description: 'Masonry grid of remote images with aspect-ratio oracle heights and bounded low-priority prefetch.',
} satisfies Config;
