import type { Config } from 'vike/types';

export default {
  title: 'Masonry Gallery | Virtual Scroll',
  description: 'Masonry grid of remote images: reserved aspect-ratio heights, bounded low-priority prefetch.',
} satisfies Config;
