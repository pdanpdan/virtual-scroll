import type { Config } from 'vike/types';

export default {
  title: 'Photo Gallery | Virtual Scroll',
  description: 'Virtualized photo grid whose placeholders hold each image space until it loads.',
} satisfies Config;
