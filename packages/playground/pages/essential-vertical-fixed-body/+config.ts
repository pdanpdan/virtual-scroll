import type { Config } from 'vike/types';

export default {
  title: 'Vertical Fixed Body | Virtual Scroll',
  description: 'Uses the main browser window for scrolling items with fixed height instead of a nested container.',
} satisfies Config;
