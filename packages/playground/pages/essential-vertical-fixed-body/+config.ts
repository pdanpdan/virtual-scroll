import type { Config } from 'vike/types';

export default {
  title: 'Vertical Fixed Body | Virtual Scroll',
  description: 'Fixed-height rows scrolled by the browser window instead of a nested container.',
} satisfies Config;
