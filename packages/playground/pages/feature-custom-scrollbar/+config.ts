import type { Config } from 'vike/types';

export default {
  title: 'Custom Scrollbar | Virtual Scroll',
  description: 'Virtual scrollbars drawn inside the container, styled with CSS variables or your own markup.',
} satisfies Config;
