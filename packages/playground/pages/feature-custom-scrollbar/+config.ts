import type { Config } from 'vike/types';

export default {
  title: 'Custom Scrollbar | Virtual Scroll',
  description: 'Demonstrates the virtual scrollbar implementation that allows the scrollbar to be designed and styled by the user.',
} satisfies Config;
