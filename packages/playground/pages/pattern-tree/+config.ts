import type { Config } from 'vike/types';

export default {
  title: 'Collapsible Tree | Virtual Scroll',
  description: 'Hierarchical list where items can be expanded or collapsed while maintaining smooth performance.',
} satisfies Config;
