import type { Config } from 'vike/types';

export default {
  title: 'Scroll Restoration | Virtual Scroll',
  description: 'Keeps the view in place when items are prepended to the list.',
} satisfies Config;
