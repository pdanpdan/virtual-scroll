import type { Config } from 'vike/types';

export default {
  title: 'Scroll Restoration | Virtual Scroll',
  description: 'Demonstrates maintaining scroll position when items are prepended to the list.',
} satisfies Config;
