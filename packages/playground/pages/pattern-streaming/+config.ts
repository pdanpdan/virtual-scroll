import type { Config } from 'vike/types';

export default {
  title: 'Live Streaming | Virtual Scroll',
  description: 'A simulated market feed that mutates only the visible window of 5,000 virtualized symbols.',
} satisfies Config;
