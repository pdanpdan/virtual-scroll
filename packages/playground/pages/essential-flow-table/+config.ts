import type { Config } from 'vike/types';

export default {
  title: 'Flow Table | Virtual Scroll',
  description: 'Virtualized rows in a real flow table with auto-sized, stable columns.',
} satisfies Config;
