import type { Config } from 'vike/types';

export default {
  title: 'Log Viewer | Virtual Scroll',
  description: 'Filter and search 200,000 lazy generated log lines with virtualization.',
} satisfies Config;
